<<<<<<< HEAD
name: Deploy Backend
=======
name: Deploy Frontend
>>>>>>> 7dc2949 (Squashed 'frontend/' content from commit cb858be)

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

<<<<<<< HEAD
      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.22'

      - name: Build Go backend
        run: |
          go build -o server

      - name: Deploy to backend folder
        run: |
          rm -f /projects/TI4-stats/server
          cp server /projects/TI4-stats/server
          chmod +x /projects/TI4-stats/server

      - name: Restart backend service
        run: |
          sudo systemctl restart ti4-backend
=======
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Build React app (ignore warnings)
        run: CI=false npm run build

      - name: Deploy to production folder
        run: |
          rm -rf /projects/TI4-frontend/build/*
          cp -r build/* /projects/TI4-frontend/build/

      - name: Restart frontend service
        run: |
          sudo systemctl restart ti4-frontend
>>>>>>> 7dc2949 (Squashed 'frontend/' content from commit cb858be)
